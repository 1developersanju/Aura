rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any signed-in user full access (demo). Publish this in Console.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
